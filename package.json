{
  "name": "docs-site",
  "version": "0.1.0",
  "private": true,
  "description": "docs.orogen.network — public documentation for the Orogen",
  "license": "Apache-2.0",
  "type": "module",
  "scripts": {
    "dev": "vitepress dev docs",
    "build": "vitepress build docs",
    "preview": "vitepress preview docs",
    "lint:links": "node scripts/lint-links.mjs",
    "test": "node scripts/lint-links.mjs"
  },
  "devDependencies": {
    "vitepress": "^1.3.0",
    "typescript": "^5.5.0"
  },
  "engines": { "node": ">=20" }
}
